# definitions-LaTeX

After pushing, a GitHub action is run to produce a main.pdf file. This ensures that the file compiles beyond just the local machine. It also creates an artifact, and a release. 

To view the latest PDF, go to the release and the `main.pdf` file should be available to view. 
